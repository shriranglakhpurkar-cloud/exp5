NAME-SHRIRANG ANIL LAKHPURKAR

PRN-25070123108

CLASS-ENTC B2

Python Dictionary – Theory & Algorithms

This repository explains the concept of dictionaries in Python using theory and algorithmic steps. It is intended for beginners to understand how dictionaries store, manage, and manipulate data in key–value pairs.

🧠 What is a Dictionary in Python?

A dictionary is a built-in data type in Python used to store data in key–value pairs.

Each key is unique

Values can be of any data type

Dictionaries are mutable, meaning their content can be changed after creation

🔑 Key Characteristics of Dictionaries

Written using curly braces {}

Data is stored as key : value

Keys must be unique

Values can be duplicated

Dictionaries are unordered (before Python 3.7)

Indexed using keys, not positions

📌 Dictionary Operations – Theory & Algorithms
1️⃣ Creating a Dictionary

Theory:
A dictionary is created by placing key–value pairs inside curly braces.

Algorithm:

Define keys and their corresponding values

Store them inside {}

Assign the structure to a variable

2️⃣ Accessing Values Using Keys

Theory:
Values in a dictionary are accessed using their keys.

Algorithm:

Identify the required key

Use the key to retrieve the value

Display or use the value

3️⃣ Duplicate Keys in Dictionary

Theory:
If a dictionary contains duplicate keys, Python keeps the last assigned value and ignores previous ones.

Algorithm:

Create a dictionary with duplicate keys

Python overwrites earlier values

Final dictionary contains unique keys only

4️⃣ Length of a Dictionary

Theory:
The length of a dictionary is the total number of key–value pairs it contains.

Algorithm:

Count the number of unique keys

Return the total count

5️⃣ Type of Dictionary

Theory:
Python provides a way to identify the data type of a dictionary.

Algorithm:

Pass the dictionary to the type-checking method

Confirm it belongs to the dictionary class

6️⃣ Updating a Dictionary

Theory:
New elements can be added or existing elements modified in a dictionary.

Algorithm:

Identify key to be added or updated

Assign a new value

Dictionary gets updated dynamically

7️⃣ Viewing All Values

Theory:
Dictionaries provide a way to view all stored values without keys.

Algorithm:

Access the value-view method

Retrieve all values

Display them as a collection

8️⃣ Removing Elements from Dictionary

Theory:
Specific key–value pairs can be removed from a dictionary.

Algorithm:

Specify the key to be removed

Delete the corresponding entry

Dictionary size reduces by one

9️⃣ Updating Existing Key Values

Theory:
If a key already exists, its value can be replaced with a new one.

Algorithm:

Locate the existing key

Assign a new value

Old value is overwritten

🔟 Searching for a Key Safely

Theory:
A safe method exists to retrieve values without causing errors if the key is absent.

Algorithm:

Take input key

Search the dictionary

Return value if found

Return default message if not found

1️⃣1️⃣ Finding Maximum Value (Topper Logic)

Theory:
Dictionaries can be used to find the key with the highest value (e.g., student with highest marks).

Algorithm:

Compare all values in the dictionary

Identify the maximum value

Retrieve the corresponding key

Display the result

🎯 Applications of Dictionary

Student records management

Product price lists

Contact books

Configuration settings

Fast data lookup

✅ Conclusion

Python dictionaries are powerful data structures that allow efficient storage and retrieval of data using keys. Understanding dictionary theory and algorithms is essential for problem-solving, real-world applications, and examinations.
